Code for basic machine learning pipeline

Use scitkit-learn estimators, and e.g. Keras estimators

Assumes there are 2 csvs, one for training/validation and one for testing

Allows for large scale experiments to compare different variations (e.g. different feature subsets, preprocessing/scaling, etc.)
